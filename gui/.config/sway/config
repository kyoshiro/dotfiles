# Default config for sway
#
### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4

# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l

# Your preferred terminal emulator
set $term foot

# Your preferred application launcher
# Note: pass the final command to swaymsg so that the resulting window can be opened
# on the original workspace that the command was run on.
set $menu dmenu_path | wofi | xargs swaymsg exec --

### Output configuration
### Workspaces Configuration
include ~/.config/sway/config.d/config.outputs

### Input configuration
include ~/.config/sway/config.d/config.inputs

### Keybinding configuration
include ~/.config/sway/config.d/config.keybindings

### SWayidle configuration
include ~/.config/sway/config.d/config.swayidle

### Appearance configuration
set $opacity 0.9
for_window [class=".*"] opacity $opacity
for_window [app_id=".*"] opacity $opacity
seat seat0 xcursor_theme breeze_cursors 36
#
# Status Bar:
#
bar {
	swaybar_command waybar
	mode hide
	position top
}

# Removing title bar
for_window [class="^.*"] border pixel 1
default_border pixel 1
default_floating_border pixel 1

# Enable window gaps
for_window [class="^.*"] border pixel 2
gaps inner 5
gaps outer 5
#smart_gaps on
#smart_borders on

### Startup Applications
include ~/.config/sway/config.d/config.startups

### Include all configs in config.d
include /etc/sway/config.d/*
